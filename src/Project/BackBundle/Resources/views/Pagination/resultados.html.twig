{%set inicio = (pagination.getCurrentPageNumber() * pagination.getItemNumberPerPage()) - 30%}
{%set fin = inicio + pagination.getItems()|length %}
{%set total = pagination.getTotalItemCount() %}
Mostrando de {{inicio}} a {{fin}} de {{total}} resultados